import { Router, type IRouter } from "express";
import { db } from "@workspace/db";
import { sql } from "drizzle-orm";
import { getCommunitySession } from "../admin/communitySession.js";
import { getLinkedSteamV2 } from "../bot/utils/linkedSteamV2.js";
import { createPixPayment, createCardPreference } from "../bot/mp.js";
import { logger } from "../lib/logger.js";
import { sendSeasonRegistrationConfirmations } from "./seasonConfirmations.js";

const router: IRouter = Router();
const PRICE = 20;
const OFFICIAL_KEY = 101;
const START = "04/09/2026 às 18:30";
const END = "30/09/2026 às 23:59";
const PIX_TYPES = new Set(["cpf","email","telefone","aleatoria"]);
const PAYMENT_SIMULATION = String(process.env.SEASON_PAYMENT_SIMULATION ?? "true").toLowerCase() !== "false";
type Profile={fullName:string;contactEmail:string;whatsapp:string;pixType:string;pixKey:string};

async function ensureTables(){
  await db.execute(sql`CREATE TABLE IF NOT EXISTS season_official_registrations (season_key INTEGER NOT NULL, discord_id TEXT NOT NULL, discord_name TEXT NOT NULL, steam_id TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'pending', amount NUMERIC(10,2) NOT NULL DEFAULT 20, mp_payment_id TEXT, mp_preference_id TEXT, full_name TEXT, contact_email TEXT, whatsapp TEXT, prize_pix_type TEXT, prize_pix_key TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), paid_at TIMESTAMPTZ, updated_at TIMESTAMPTZ NOT NULL DEFAULT now(), PRIMARY KEY(season_key,discord_id))`);
  for(const c of ["full_name TEXT","contact_email TEXT","whatsapp TEXT","prize_pix_type TEXT","prize_pix_key TEXT","confirmation_email_sent_at TIMESTAMPTZ","confirmation_whatsapp_sent_at TIMESTAMPTZ","confirmation_email_status TEXT","confirmation_whatsapp_status TEXT","confirmation_last_error TEXT"]){await db.execute(sql.raw(`ALTER TABLE season_official_registrations ADD COLUMN IF NOT EXISTS ${c}`));}
}
function clean(v:unknown,max:number){return String(v??"").trim().slice(0,max)}
function profileFrom(body:any):Profile|{error:string}{
  const fullName=clean(body?.fullName,120),contactEmail=clean(body?.contactEmail||body?.email,160).toLowerCase(),whatsapp=clean(body?.whatsapp,30),pixType=clean(body?.pixType,20).toLowerCase(),pixKey=clean(body?.pixKey,180);
  if(fullName.length<5||!fullName.includes(" "))return {error:"Informe seu nome completo."};
  if(!/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/.test(contactEmail))return {error:"Informe um e-mail de contato válido."};
  const phoneDigits=whatsapp.replace(/\D/g,"");if(phoneDigits.length<10||phoneDigits.length>13)return {error:"Informe um WhatsApp válido com DDD."};
  if(!PIX_TYPES.has(pixType))return {error:"Selecione o tipo da chave PIX para premiação."};
  if(!pixKey)return {error:"Informe a chave PIX que será usada caso você ganhe."};
  if(pixType==="cpf"&&pixKey.replace(/\D/g,"").length!==11)return {error:"A chave PIX CPF deve possuir 11 dígitos."};
  if(pixType==="email"&&!/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/.test(pixKey))return {error:"Informe uma chave PIX do tipo e-mail válida."};
  if(pixType==="telefone"){const d=pixKey.replace(/\D/g,"");if(d.length<10||d.length>13)return {error:"Informe uma chave PIX telefone válida."};}
  return {fullName,contactEmail,whatsapp,pixType,pixKey};
}
async function saveProfile(discordId:string,discordName:string,steamId:string,p:Profile){await ensureTables();await db.execute(sql`INSERT INTO season_official_registrations(season_key,discord_id,discord_name,steam_id,status,amount,full_name,contact_email,whatsapp,prize_pix_type,prize_pix_key,updated_at) VALUES(${OFFICIAL_KEY},${discordId},${discordName},${steamId},'pending',${PRICE},${p.fullName},${p.contactEmail},${p.whatsapp},${p.pixType},${p.pixKey},now()) ON CONFLICT(season_key,discord_id) DO UPDATE SET discord_name=EXCLUDED.discord_name,steam_id=EXCLUDED.steam_id,full_name=EXCLUDED.full_name,contact_email=EXCLUDED.contact_email,whatsapp=EXCLUDED.whatsapp,prize_pix_type=EXCLUDED.prize_pix_type,prize_pix_key=EXCLUDED.prize_pix_key,updated_at=now()`)}
async function mpPayment(id:string){const token=String(process.env.MP_ACCESS_TOKEN||"");if(!token)return null;const r=await fetch(`https://api.mercadopago.com/v1/payments/${encodeURIComponent(id)}`,{headers:{Authorization:`Bearer ${token}`}});return r.ok?await r.json() as any:null;}
async function finishActivation(discordId:string,paymentId:string){await ensureTables();await db.execute(sql`UPDATE season_official_registrations SET status='active',mp_payment_id=${paymentId},paid_at=COALESCE(paid_at,now()),updated_at=now() WHERE season_key=${OFFICIAL_KEY} AND discord_id=${discordId}`);const delivery=await sendSeasonRegistrationConfirmations(discordId);return {ok:true,delivery};}
async function activateReal(discordId:string,paymentId:string){const p=await mpPayment(paymentId);if(!p||String(p.status)!=="approved")return {ok:false};const metadata=p.metadata||{};if(String(metadata.discord_user_id||"")!==discordId||String(metadata.vip_tier||"")!=="season1_entry")return {ok:false};return finishActivation(discordId,paymentId);}

router.get("/season/1/inscricao-oficial/status",async(req,res)=>{const session=getCommunitySession(req);if(!session)return void res.status(401).json({ok:false,authenticated:false});await ensureTables();const r:any=await db.execute(sql`SELECT status,amount,created_at,paid_at,confirmation_email_status,confirmation_whatsapp_status,(full_name IS NOT NULL AND contact_email IS NOT NULL AND whatsapp IS NOT NULL AND prize_pix_type IS NOT NULL AND prize_pix_key IS NOT NULL) profile_complete FROM season_official_registrations WHERE season_key=${OFFICIAL_KEY} AND discord_id=${session.userId} LIMIT 1`);return void res.json({ok:true,paymentSimulation:PAYMENT_SIMULATION,registered:r?.rows?.[0]?.status==='active',registration:r?.rows?.[0]||null});});
router.post("/season/1/inscricao-oficial/dados",async(req,res)=>{const session=getCommunitySession(req);if(!session)return void res.status(401).json({error:"Entre com Discord para continuar."});const linked=await getLinkedSteamV2(session.userId);if(!linked?.steamId)return void res.status(409).json({error:"Vincule sua Steam antes de concluir a inscrição."});const parsed=profileFrom(req.body);if("error" in parsed)return void res.status(400).json({error:parsed.error});await saveProfile(session.userId,session.username,linked.steamId,parsed);return void res.json({ok:true});});
router.post("/season/1/inscricao-oficial/simular-pagamento",async(req,res)=>{if(!PAYMENT_SIMULATION)return void res.status(404).json({error:"Simulação desativada."});const session=getCommunitySession(req);if(!session)return void res.status(401).json({error:"Entre com Discord para continuar."});const linked=await getLinkedSteamV2(session.userId);if(!linked?.steamId)return void res.status(409).json({error:"Vincule sua Steam antes de testar a inscrição."});const parsed=profileFrom(req.body);if("error" in parsed)return void res.status(400).json({error:parsed.error});await saveProfile(session.userId,session.username,linked.steamId,parsed);const id=`SIM-SEASON1-${Date.now()}-${session.userId}`;const result=await finishActivation(session.userId,id);return void res.json({...result,paymentId:id,simulated:true});});
router.post("/season/1/inscricao-oficial/pix",async(req,res)=>{if(PAYMENT_SIMULATION)return void res.status(409).json({error:"Pagamento real está desativado. Use a simulação."});const session=getCommunitySession(req);if(!session)return void res.status(401).json({error:"Entre com Discord para se inscrever."});const linked=await getLinkedSteamV2(session.userId);if(!linked?.steamId)return void res.status(409).json({error:"Vincule sua Steam antes de pagar a inscrição."});const parsed=profileFrom(req.body);if("error" in parsed)return void res.status(400).json({error:parsed.error});await saveProfile(session.userId,session.username,linked.steamId,parsed);const payment=await createPixPayment({amount:PRICE,description:"Inscrição Season 1 Guerra Fria",email:parsed.contactEmail,discordUserId:session.userId,steamId:linked.steamId,vipTier:"season1_entry"});if("error" in payment)return void res.status(502).json({error:payment.error});await db.execute(sql`UPDATE season_official_registrations SET status='pending',amount=${PRICE},mp_payment_id=${payment.paymentId},updated_at=now() WHERE season_key=${OFFICIAL_KEY} AND discord_id=${session.userId}`);return void res.json({ok:true,paymentId:payment.paymentId,qrCode:payment.qrCode,qrCodeBase64:payment.qrCodeBase64,expiresAt:payment.expiresAt});});
router.post("/season/1/inscricao-oficial/card",async(req,res)=>{if(PAYMENT_SIMULATION)return void res.status(409).json({error:"Pagamento real está desativado. Use a simulação."});const session=getCommunitySession(req);if(!session)return void res.status(401).json({error:"Entre com Discord para se inscrever."});const linked=await getLinkedSteamV2(session.userId);if(!linked?.steamId)return void res.status(409).json({error:"Vincule sua Steam antes de pagar a inscrição."});const parsed=profileFrom(req.body);if("error" in parsed)return void res.status(400).json({error:parsed.error});await saveProfile(session.userId,session.username,linked.steamId,parsed);const preference=await createCardPreference({amount:PRICE,title:"Inscrição Season 1 Guerra Fria",discordUserId:session.userId,steamId:linked.steamId,vipTier:"season1_entry"});if(!preference)return void res.status(502).json({error:"Não foi possível abrir o checkout."});await db.execute(sql`UPDATE season_official_registrations SET status='pending',amount=${PRICE},mp_preference_id=${preference.preferenceId},updated_at=now() WHERE season_key=${OFFICIAL_KEY} AND discord_id=${session.userId}`);return void res.json({ok:true,checkoutUrl:preference.checkoutUrl});});
router.post("/season/1/inscricao-oficial/confirmar",async(req,res)=>{const session=getCommunitySession(req);if(!session)return void res.status(401).json({ok:false});const id=String(req.body?.paymentId||"");if(!id)return void res.status(400).json({ok:false});try{return void res.json(await activateReal(session.userId,id));}catch(error){logger.error({error},"season official payment confirmation failed");return void res.status(500).json({ok:false});}});

router.get("/season/1/inscricao-oficial",async(req,res)=>{
  const session=getCommunitySession(req);if(!session)return void res.redirect("/api/admin/auth/login?target=season");
  await ensureTables();
  const r:any=await db.execute(sql`SELECT status,steam_id,full_name,contact_email,whatsapp,prize_pix_type,prize_pix_key,confirmation_email_status,confirmation_whatsapp_status FROM season_official_registrations WHERE season_key=${OFFICIAL_KEY} AND discord_id=${session.userId} LIMIT 1`);
  const row=r?.rows?.[0]||null;const active=row?.status==='active';const profileComplete=Boolean(row?.full_name&&row?.contact_email&&row?.whatsapp&&row?.prize_pix_type&&row?.prize_pix_key);
  const esc=(v:any)=>String(v||'').replace(/["&<>]/g,'');const safe=esc(session.username);const steamId=esc(row?.steam_id||'');
  res.setHeader("Cache-Control","no-store");

  if(active&&profileComplete){
    return void res.type("html").send(`<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#080a0d"><title>Meu perfil • Season 1</title><style>
*{box-sizing:border-box}body{margin:0;background:#080a0d;color:#f8fafc;font-family:Inter,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;min-height:100vh}.w{width:min(900px,calc(100% - 24px));margin:auto;padding:18px 0 80px}.back{display:inline-flex;color:#aab2bd;text-decoration:none;font-size:11px;font-weight:850;margin:7px 0 14px}.hero{border:1px solid #24543c;border-radius:25px;padding:26px;background:radial-gradient(circle at 90% 0,#22c55e1f,transparent 34%),linear-gradient(145deg,#0d2018,#0d1116 66%)}.tag{display:inline-flex;border:1px solid #22c55e4d;background:#22c55e12;color:#86efac;border-radius:999px;padding:7px 10px;font-size:9px;font-weight:1000;letter-spacing:.12em}.hero h1{font-size:clamp(34px,8vw,58px);line-height:.96;letter-spacing:-.05em;margin:12px 0 7px}.hero p{color:#aab2bd;line-height:1.55;font-size:12px}.profile{margin-top:14px;border:1px solid #2d333c;background:linear-gradient(180deg,#11161c,#0b0f13);border-radius:22px;padding:22px}.top{display:grid;grid-template-columns:120px 1fr;gap:18px;align-items:center}.rankImg{width:110px;height:126px;object-fit:contain;filter:drop-shadow(0 12px 18px #000b)}.label{font-size:9px;color:#7f8895;letter-spacing:.12em;font-weight:900}.rankName{font-size:28px;font-weight:1000;margin:4px 0}.xp{font-size:17px;color:#fbbf24;font-weight:950}.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:9px;margin:18px 0}.stat{border:1px solid #303741;background:#0c1116;border-radius:14px;padding:13px}.stat small{display:block;color:#777f8a;font-size:8px;letter-spacing:.1em}.stat b{display:block;margin-top:5px;font-size:16px}.barWrap{margin-top:16px}.barHead{display:flex;justify-content:space-between;gap:10px;align-items:end;margin-bottom:8px}.barHead b{font-size:12px}.barHead span{color:#9aa2ad;font-size:10px}.bar{height:14px;background:#222831;border-radius:99px;overflow:hidden}.fill{height:100%;width:0;background:linear-gradient(90deg,#ef4444,#f59e0b);transition:width .5s ease}.progressText{color:#a7afb9;font-size:11px;line-height:1.5;margin-top:9px}.seasonInfo{display:grid;grid-template-columns:1fr 1fr;gap:9px;margin-top:14px}.info{border:1px solid #2d333c;background:#0c1015;border-radius:14px;padding:13px}.info small{display:block;color:#777f89;font-size:8px}.info b{display:block;margin-top:5px;font-size:11px}.actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:16px}.btn{display:inline-flex;justify-content:center;text-decoration:none;border:1px solid #3a414b;background:#15191f;color:#fff;border-radius:11px;padding:12px 14px;font-size:10px;font-weight:950}.btn.primary{background:#ef4444;border-color:#ef4444}.loading{color:#8d96a2;font-size:12px;padding:10px 0}.error{color:#fca5a5}
@media(max-width:620px){.top{grid-template-columns:92px 1fr}.rankImg{width:86px;height:100px}.rankName{font-size:23px}.stats{grid-template-columns:1fr 1fr}.seasonInfo{grid-template-columns:1fr}.actions .btn{width:100%}}
</style></head><body><main class="w"><a class="back" href="/season1">← Voltar para a Season</a><section class="hero"><span class="tag">✓ INSCRIÇÃO CONFIRMADA • SEASON 1</span><h1>Meu perfil da Season</h1><p>Olá, <b>${safe}</b>. Sua vaga está confirmada. A partir daqui esta página mostra somente seu perfil competitivo e sua progressão — nenhuma etapa de pagamento será exibida novamente.</p></section><section class="profile"><div id="loading" class="loading">Carregando sua patente e posição no ranking...</div><div id="content" style="display:none"><div class="top"><img class="rankImg" id="rankImg" alt="Patente atual"><div><div class="label">PATENTE ATUAL</div><div class="rankName" id="rankName">Soldado</div><div class="xp" id="xp">0 XP</div></div></div><div class="stats"><div class="stat"><small>POSIÇÃO ATUAL</small><b id="position">#—</b></div><div class="stat"><small>XP ATUAL</small><b id="xpStat">0</b></div><div class="stat"><small>PRÓXIMA PATENTE</small><b id="nextRank">—</b></div></div><div class="barWrap"><div class="barHead"><b>Progresso para a próxima patente</b><span id="percent">0%</span></div><div class="bar"><div class="fill" id="fill"></div></div><div class="progressText" id="progressText"></div></div><div class="seasonInfo"><div class="info"><small>STATUS</small><b>Participante confirmado</b></div><div class="info"><small>INÍCIO DA SEASON 1</small><b>${START}</b></div></div><div class="actions"><a class="btn primary" href="/season1#ranking">VER RANKING COMPLETO</a><a class="btn" href="/season1/guia">COMO GANHAR XP</a></div></div><div id="err" class="error"></div></section></main><script>
const f=v=>Number(v||0).toLocaleString('pt-BR');
(async()=>{try{const r=await fetch('/api/season/1/player/${steamId}?_='+Date.now(),{cache:'no-store'});if(!r.ok)throw new Error('Não foi possível carregar seu perfil agora.');const d=await r.json(),p=d.player;if(!p)throw new Error('Seu perfil ainda não apareceu no ranking da Season. Assim que o servidor registrar sua atividade, ele será exibido aqui.');document.getElementById('loading').style.display='none';document.getElementById('content').style.display='block';document.getElementById('rankImg').src=p.patente_imagem||'';document.getElementById('rankName').textContent=p.patente||'Soldado';document.getElementById('xp').textContent=f(p.xp)+' XP';document.getElementById('xpStat').textContent=f(p.xp)+' XP';document.getElementById('position').textContent='#'+f(p.position);document.getElementById('nextRank').textContent=p.proxima_patente||'General Frio';const pct=Math.max(0,Math.min(100,Number(p.progresso_percentual||0)));document.getElementById('fill').style.width=pct+'%';document.getElementById('percent').textContent=pct+'%';document.getElementById('progressText').textContent=p.proxima_patente?'Faltam '+f(p.xp_faltante)+' XP para '+p.proxima_patente:(p.general_frio?'Você é o General Frio atual — Marechal e Top 1 da Season.':(p.proxima_condicao||'Marechal alcançado. Agora dispute o Top 1 para conquistar General Frio.'));}catch(e){document.getElementById('loading').style.display='none';document.getElementById('err').textContent=e.message;}})();
</script></body></html>`);
  }

  return void res.type("html").send(`<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#090b0e"><title>Inscrição Season 1 • Guerra Fria</title><style>
*{box-sizing:border-box}body{margin:0;background:#080a0d;color:#f8fafc;font-family:Inter,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;min-height:100vh}.w{width:min(980px,calc(100% - 24px));margin:auto;padding:18px 0 80px}.back{display:inline-flex;color:#aab2bd;text-decoration:none;font-size:11px;font-weight:850;margin:7px 0 14px}.hero{border:1px solid #4a2628;border-radius:26px;padding:30px;background:radial-gradient(circle at 90% 0,#ef444438,transparent 34%),linear-gradient(145deg,#251012,#101319 64%)}.tag{display:inline-flex;border:1px solid #ef44445c;background:#ef444414;color:#fecaca;border-radius:999px;padding:7px 10px;font-size:9px;font-weight:1000;letter-spacing:.13em}.heroGrid{display:grid;grid-template-columns:1fr 220px;gap:24px;align-items:center}.hero h1{font-size:clamp(38px,8vw,67px);line-height:.93;letter-spacing:-.055em;margin:12px 0}.hero p{color:#b8bec8;line-height:1.6;font-size:13px}.priceBox{border:1px solid #77402f;background:#1b120e;border-radius:20px;padding:19px;text-align:center}.priceBox small{font-size:9px;color:#a9afb8}.priceBox strong{display:block;font-size:43px;color:#fbbf24;margin:6px 0}.layout{display:grid;grid-template-columns:1.35fr .65fr;gap:12px;align-items:start;margin-top:14px}.card{border:1px solid #2d333c;background:#10141a;border-radius:19px;padding:20px;margin-bottom:12px}.card h2{margin:0 0 5px;font-size:20px}.card p{color:#9fa7b2;line-height:1.55;font-size:11px}.grid{display:grid;grid-template-columns:1fr 1fr;gap:11px}.lab{display:block;font-size:9px;color:#c5cbd4;font-weight:900;margin-bottom:6px}.field{width:100%;padding:13px 12px;border-radius:11px;border:1px solid #343b45;background:#090c10;color:white;font-size:16px}.full{grid-column:1/-1}.privacy{color:#808894!important;font-size:10px!important}.summaryRow{display:flex;justify-content:space-between;gap:10px;padding:11px 0;border-top:1px solid #ffffff0d;font-size:11px}.summaryRow span{color:#8e96a1}.summaryRow.total strong{font-size:20px;color:#fbbf24}.btn{width:100%;border:1px solid #ef4444;border-radius:12px;padding:15px;font-weight:1000;background:#ef4444;color:white;font-size:12px}.warn{color:#fca5a5;font-size:11px;min-height:18px;margin-top:9px;text-align:center}.test{border-color:#775317;background:#23180a}.test b{color:#fde68a}.dates{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px}.date{border:1px solid #3a3135;background:#120f12;border-radius:12px;padding:11px}.date small{display:block;color:#7d858f;font-size:8px}.date b{display:block;margin-top:4px;font-size:11px}@media(max-width:760px){.hero{padding:22px 18px}.heroGrid,.layout,.grid{grid-template-columns:1fr}.full{grid-column:auto}.priceBox{text-align:left}}
</style></head><body><main class="w"><a class="back" href="/season1">← Voltar para a Season</a><section class="hero"><div class="heroGrid"><div><span class="tag">🏆 INSCRIÇÕES OFICIAIS ABERTAS</span><h1>Guerra Fria<br>Season 1</h1><p>Finalize sua inscrição para entrar na disputa oficial.</p><div class="dates"><div class="date"><small>INÍCIO</small><b>${START}</b></div><div class="date"><small>ENCERRAMENTO</small><b>${END}</b></div></div></div><div class="priceBox"><small>VALOR DA INSCRIÇÃO</small><strong>R$ 20</strong><span>pagamento único • Season 1</span></div></div></section>${PAYMENT_SIMULATION?`<section class="card test" style="margin-top:14px"><b>🧪 Ambiente de teste ativo</b><p>Nenhuma cobrança real será realizada.</p></section>`:''}<div class="layout"><section class="card"><h2>Dados do participante</h2><p>Esses dados serão associados à sua inscrição oficial.</p><div class="grid"><div><label class="lab">NOME COMPLETO</label><input class="field" id="fullName" value="${esc(row?.full_name)}"></div><div><label class="lab">E-MAIL DE CONTATO</label><input class="field" id="contactEmail" type="email" value="${esc(row?.contact_email)}"></div><div><label class="lab">WHATSAPP COM DDD</label><input class="field" id="whatsapp" value="${esc(row?.whatsapp)}"></div><div><label class="lab">TIPO DA CHAVE PIX PARA PRÊMIO</label><select class="field" id="pixType"><option value="">Selecione</option><option value="cpf">CPF</option><option value="email">E-mail</option><option value="telefone">Telefone</option><option value="aleatoria">Chave aleatória</option></select></div><div class="full"><label class="lab">CHAVE PIX PARA PREMIAÇÃO</label><input class="field" id="pixKey" value="${esc(row?.prize_pix_key)}"></div></div><p class="privacy">🔒 Seus dados são privados.</p></section><aside><section class="card"><h2>Resumo</h2><div class="summaryRow"><span>Produto</span><strong>Season 1</strong></div><div class="summaryRow total"><span>Total</span><strong>R$ 20,00</strong></div><button class="btn" id="pay">${PAYMENT_SIMULATION?'SIMULAR APROVAÇÃO • R$ 20':'CONTINUAR PARA O PAGAMENTO'}</button><div id="out" class="warn"></div></section></aside></div></main><script>const out=document.getElementById('out'),btn=document.getElementById('pay');const saved=${JSON.stringify(String(row?.prize_pix_type||''))};if(saved)document.getElementById('pixType').value=saved;function payload(){return{fullName:document.getElementById('fullName').value,contactEmail:document.getElementById('contactEmail').value,whatsapp:document.getElementById('whatsapp').value,pixType:document.getElementById('pixType').value,pixKey:document.getElementById('pixKey').value}}async function post(url,body){const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.error||'Não foi possível continuar.');return d}btn.onclick=async()=>{btn.disabled=true;try{out.textContent='Processando...';${PAYMENT_SIMULATION?`await post('/api/season/1/inscricao-oficial/simular-pagamento',payload());setTimeout(()=>location.reload(),700);`:`out.textContent='Pagamento real temporariamente indisponível nesta interface.'`}}catch(e){out.textContent=e.message;btn.disabled=false}}</script></body></html>`);
});

export default router;
